spring:
  application:
    name: numeric-generate-service
  data:
    redis:
      host: redis
      port: 6379
      username: default
      password: supersecretpassword

redis:
  key: orders_code_5

springdoc:
  api-docs:
    path: /v1/api-docs
    enabled: true
  swagger-ui:
    enabled: true